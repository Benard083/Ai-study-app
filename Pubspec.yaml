name: ai_study_elite_v2

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  firebase_core: ^2.30.0
  cloud_firestore: ^4.15.0
  google_mobile_ads: ^4.0.0

flutter:
  uses-material-design: true
