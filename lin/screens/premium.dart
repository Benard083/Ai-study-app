import 'package:flutter/material.dart';

class PremiumScreen extends StatelessWidget {
  @override
  Widget build(context) {
    return Scaffold(
      appBar: AppBar(title: Text("Go Premium")),
      body: Center(
        child: Text("Unlock unlimited AI access"),
      ),
    );
  }
}
